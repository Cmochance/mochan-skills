# 实例/敏感配置(每个 skill 本地填,authoritative 不入库)
**/config.local.*

# 外部拷贝的 skill(非自建,不收录)
ci-fix/
# lark-cli 安装的官方 skill symlink(指向 ~/.agents/skills/,由 lark-cli update 管理)
lark-*

# 本地知识库 / 生成数据(内部,不公开)
transfer-bug-recorder/records/

# 杂项
.DS_Store
*.log
